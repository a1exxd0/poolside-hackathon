[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "poolside-cache"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["hierarchy", "kv_cache", "benchmark"]
